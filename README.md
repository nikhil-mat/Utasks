# Ulauncher extention to quickly add caldav tasks

Currently only supports caldav servers with no passwords(for eg: a local server)

requires: 
[caldav](https://pypi.org/project/caldav/) package from pypi
